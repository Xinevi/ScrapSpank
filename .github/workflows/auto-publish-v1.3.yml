name: Auto-publish Release v1.3

on:
  schedule:
    - cron: '0 15 12 8 *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const { data: releases } = await github.rest.repos.listReleases({ owner, repo, per_page: 100 });
            const draft = releases.find(r => r.draft && r.name === 'Release v1.3');
            if (!draft) {
              console.log('Draft "Release v1.3" not found, nothing to publish.');
              return;
            }
            await github.rest.repos.updateRelease({
              owner, repo, release_id: draft.id,
              tag_name: 'v1.3', name: 'Release v1.3',
              draft: false, prerelease: false, make_latest: true
            });
            console.log('Published Release v1.3 (id=' + draft.id + ') with tag v1.3');
